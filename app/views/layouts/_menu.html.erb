<div id="menu-holder">
	<div class="wrapper">
		<ul id="nav" class="sf-menu">
			<li class="current-menu-item"><%= link_to "Home", root_url %></li>
			<% if session[:user] != nil %>
				<li><%= link_to "Users", root_url + "users/" %></li>
			<% end %>
			<% if session[:user] != nil %>
				<li><%= link_to "Fingerings", root_url + "fingerings" %></li>
			<% end %>
		</ul>
	</div>
</div>